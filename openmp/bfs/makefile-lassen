# C compiler
CC = clang++

CC_FLAGS = -g -Wall -fopenmp -O2

all: bfs bfs_offload

bfs: bfs.cpp 
	$(CC) $(CC_FLAGS) bfs.cpp -o bfs 

bfs_offload: bfs.cpp
	$(CC) $(CC_FLAGS) -fopenmp-targets=nvptx64 bfs.cpp -o bfs_offload

clean:
	rm -f bfs bfs_offload result*.txt
